import { ChangTextDirective } from './chang-text.directive';

describe('ChangTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangTextDirective();
    expect(directive).toBeTruthy();
  });
});
